```xml
        <?xml version="1.0" encoding="UTF-8"?>

        <!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">

<suite name="Calibo API Automation Suite"
       verbose="2"
       parallel="false">

    <!-- Framework Parameters -->
    <parameter name="environment" value="qa"/>
    <parameter name="browser" value="chrome"/>
    <parameter name="headless" value="true"/>

    <!-- Listener (Optional - Uncomment if required) -->
    <!--
    <listeners>
        <listener class-name="listeners.TestListener"/>
    </listeners>
    -->

    <test name="Calibo API Tests">

        <classes>

            <!-- User Module -->
            <class name="tests.UsersTests"/>

            <!-- Project Module -->
            <!--
            <class name="tests.ProjectsTests"/>
            -->

            <!-- Pipeline Module -->
            <!--
            <class name="tests.PipelinesTests"/>
            -->

            <!-- Assessment Module -->
            <!--
            <class name="tests.AssessmentsTests"/>
            -->

            <!-- Ideas Module -->
            <!--
            <class name="tests.IdeasTests"/>
            -->

        </classes>

    </test>

</suite>
        ```
